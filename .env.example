# Обязательно укажи /api/v4 в конце
GITLAB_BASE_URL=https://gitlab.example.com/api/v4
# Персональный токен (api scope)
GITLAB_TOKEN=glpat-XXXXXXXXXXXXXXXXXXXXXXXX

# Необязательные параметры
GITLAB_PER_PAGE=100
REQUEST_TIMEOUT_S=30

# UI автообновление (в секундах)
UI_AUTO_REFRESH_ENABLED=true
UI_AUTO_REFRESH_SEC=15

# Переписывать абсолютные редиректы GitLab на относительные (для base_url)
GITLAB_REWRITE_REDIRECTS=true

# Минимальный уровень доступа для проектов, чтобы видеть их в списке (40=Maintainer, 50=Owner)
GITLAB_MIN_ACCESS_LEVEL=40

# Логирование
# Уровень логов приложения: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO
# Детализация логов HTTP-запросов к GitLab
# DEBUG — все запросы/ответы; INFO — только факты операций; WARNING — только ошибки
GITLAB_LOG_LEVEL=WARNING
